from django.apps import AppConfig


class GrabsomoreConfig(AppConfig):
    """Configuration for the grabsomore authentication app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'grabsomore'
